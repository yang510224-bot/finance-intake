# ─────────────────────────────────────────────
# Supabase（新建專案步驟）
# 1. 前往 https://supabase.com → New Project
# 2. 建立後進入 Settings → API
# 3. 複製 Project URL → VITE_SUPABASE_URL & SUPABASE_URL
# 4. 複製 anon public key → VITE_SUPABASE_ANON_KEY
# 5. 複製 service_role key（勿外洩）→ SUPABASE_SERVICE_KEY
# 6. 在 Supabase SQL Editor 執行 supabase/schema.sql
# ─────────────────────────────────────────────

# 前端用（Vite 會打包進 browser bundle，僅 anon key 安全）
VITE_SUPABASE_URL=https://xxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# 後端 Serverless Function 用（絕對不可提交 .env 本身）
SUPABASE_URL=https://xxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Claude API（前往 https://console.anthropic.com → API Keys）
ANTHROPIC_API_KEY=sk-ant-api03-...

# ─────────────────────────────────────────────
# LINE Messaging API（問卷提交通知）
# 1. LINE_CHANNEL_ACCESS_TOKEN: 既有 line-bot-prod 的 Channel Access Token
# 2. LINE_ADMIN_USER_ID: Howard 個人的 LINE User ID
# 
# 如何取得 Howard 個人的 LINE User ID：
# 當用戶傳訊息給 LINE@ 時，webhook 會收到該用戶的 userId。
# Howard 只要傳一則訊息給自己的 LINE@，接著去 Vercel Dashboard 看 webhook 的 log，
# 從中找到自己的 userId (通常是 U 開頭的一個字串) 填入下方即可。
# ─────────────────────────────────────────────
LINE_CHANNEL_ACCESS_TOKEN=your_line_channel_access_token
LINE_ADMIN_USER_ID=your_line_admin_user_id
